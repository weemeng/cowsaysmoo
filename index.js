const cows = require ("cows")(); //calling the cows function with the ()
const index = process.argv[2] || 0;
console.log(cows[index]);

Math.random * 5;

//console.log(Math.max(...cows.map(el=>el.length)));